# from django.conf import settings

# def show_toolbar(request):
#     return settings.DEBUG and (request.GET.get("djdt") == "1" or (getattr(request, "user", None) and request.user.is_staff))

# # TEMP: force-on to verify wiring; point settings to this and reload
# def always(request):
#     return True
